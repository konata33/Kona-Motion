import { withInstall } from '../utils/components'
import Sunset from './Sunset'
import { props as sunsetProps } from './props'

withInstall(Sunset)

export { sunsetProps }

export const _SunsetComponent = Sunset

export default Sunset
